# ---------------------------------------------------------------------------
# Concord environment template.
#
# Copy to `.env.local` and fill in real values. `.env.local` is git-ignored
# and must NEVER be committed. This file contains NAMES and safe local
# examples only — no real secrets.
# ---------------------------------------------------------------------------

# --- Clerk (identity) -------------------------------------------------------
# Publishable key is safe for the browser (NEXT_PUBLIC_ prefix).
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_replace_me

# Server-only Clerk secret key. NEVER expose to the client bundle.
CLERK_SECRET_KEY=sk_test_replace_me

# --- PostgreSQL (Phase 1) ---------------------------------------------------
# Local development database (Docker Compose service "db").
# The credentials below are the compose-file defaults: non-production,
# local-machine only.
DATABASE_URL=postgres://concord:concord_local_dev@localhost:5433/concord

# Isolated test database used by integration tests (never dev data).
DATABASE_TEST_URL=postgres://concord:concord_local_dev@localhost:5433/concord_test
