//! Wire protocol: frames, codec, golden fixtures (P3-M011..M012).
//!
//! Filled in from M011; exists so the workspace skeleton compiles from M008.

/// Wire protocol version implemented by this gateway (PROTOCOL §9.1).
pub const WIRE_VERSION: u32 = 1;
