/**
 * Default page margins for the editor viewport, in CSS pixels.
 *
 * 56px matches the default padding of a US-Letter page at 100% zoom in
 * the print-inspired document layout; the ruler and the collaboration
 * provider both position the text column from these values. Users may
 * drag the rulers to override them (persisted in localStorage by the
 * provider); these are only the initial/reset values.
 */
export const LEFT_MARGIN_DEFAULT = 56;
export const RIGHT_MARGIN_DEFAULT = 56;
